const DEFAULT_MODEL='gpt-4o-mini';module.exports=async function(req,res){cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return res.status(405).json({ok:false,error:'Método no permitido.'});try{const{text}=req.body||{};if(!text)return res.status(400).json({ok:false,error:'Texto requerido.'});const key=process.env.OPENAI_API_KEY;if(!key)throw new Error('Falta OPENAI_API_KEY.');const prompt=`Mejore el siguiente texto para atención al cliente. Mantenga exactamente la intención original. Use un tono profesional, amable, claro y natural. Hable siempre de usted. Está prohibido tutear o usar formas como tú, puedes, debes, tienes, envíame, dime, cuéntame, intenta, ingresa o selecciona cuando se dirijan al cliente. No invente información. Devuelva únicamente el texto mejorado.\n\n${text}`;const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||DEFAULT_MODEL,input:prompt,temperature:.2,max_output_tokens:900})});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'Error de IA.');return res.status(200).json({ok:true,improvedText:String(d.output_text||'').trim()});}catch(e){return res.status(500).json({ok:false,error:e.message});}};function cors(res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','POST, OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');}
