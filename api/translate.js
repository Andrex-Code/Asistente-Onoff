const DEFAULT_MODEL='gpt-4o-mini';module.exports=async function(req,res){cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return res.status(405).json({ok:false,error:'Método no permitido.'});try{const{text,direction}=req.body||{};if(!text)return res.status(400).json({ok:false,error:'Texto requerido.'});const source=direction==='pt-es'?'portugués brasileño':'español';const target=direction==='pt-es'?'español colombiano natural':'portugués brasileño natural';const translatedText=await ask(`Traduce de ${source} a ${target}. Devuelve solo la traducción, sin explicaciones:\n\n${text}`);return res.status(200).json({ok:true,translatedText});}catch(e){return res.status(500).json({ok:false,error:e.message});}};async function ask(prompt){const key=process.env.OPENAI_API_KEY;if(!key)throw new Error('Falta OPENAI_API_KEY.');const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||DEFAULT_MODEL,input:prompt,temperature:.2,max_output_tokens:1200})});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'Error de IA.');return String(d.output_text||'').trim();}function cors(res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','POST, OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');}
